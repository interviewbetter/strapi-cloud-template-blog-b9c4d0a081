'use strict';

/**
 * os-enterprise-organisation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::os-enterprise-organisation.os-enterprise-organisation');
