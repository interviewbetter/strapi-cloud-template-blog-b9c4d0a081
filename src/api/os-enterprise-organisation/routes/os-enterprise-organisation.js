'use strict';

/**
 * os-enterprise-organisation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::os-enterprise-organisation.os-enterprise-organisation');
