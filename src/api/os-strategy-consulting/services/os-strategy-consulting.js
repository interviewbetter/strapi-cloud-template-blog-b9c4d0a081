'use strict';

/**
 * os-strategy-consulting service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::os-strategy-consulting.os-strategy-consulting');
