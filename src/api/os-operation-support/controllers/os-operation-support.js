'use strict';

/**
 * os-operation-support controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::os-operation-support.os-operation-support');
