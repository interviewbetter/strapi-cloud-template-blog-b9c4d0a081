'use strict';

/**
 * os-delivery-model service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::os-delivery-model.os-delivery-model');
