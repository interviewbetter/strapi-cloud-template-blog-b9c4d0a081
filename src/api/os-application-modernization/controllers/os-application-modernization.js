'use strict';

/**
 * os-application-modernization controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::os-application-modernization.os-application-modernization');
