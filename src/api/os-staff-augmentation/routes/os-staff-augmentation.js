'use strict';

/**
 * os-staff-augmentation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::os-staff-augmentation.os-staff-augmentation');
