'use strict';

/**
 * os-staff-augmentation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::os-staff-augmentation.os-staff-augmentation');
